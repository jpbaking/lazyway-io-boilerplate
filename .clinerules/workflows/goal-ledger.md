# Use the skill

Use the `goal-ledger` skill for this request. Follow its `SKILL.md`
instructions exactly. Use relevant context already supplied by the user and
ask for any required input that is missing.
